# Test if a node can point to itself

# Create a graph
created graph graphWithANode

# Add a node
added node NodeA to graphWithANode

# Add an edge
added edge label from NodeA to NodeA in graphWithANode

# Print the nodes in the graph and the outgoing edges 
graphWithANode contains: NodeA 
the children of NodeA in graphWithANode are: NodeA(label)